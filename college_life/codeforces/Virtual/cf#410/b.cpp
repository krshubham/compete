#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <set>
using namespace std;

typedef long long ll;
typedef long long int lli;
typedef unsigned long long int ulli;

void solve(vector<string> v,lli ss){
	// std::rotate(s.begin(), s.begin() + 1, s.end());
	lli max = LLONG_MAX;

	for (int i = 0; i < ss; ++i){
		for (int j = 0; j < 0; ++j){
			// lengthy part with 2 seconds time. Its doable
		}
	}
}

lli comp(string s,set v){

}

int main(int argc, char const *argv[]){
	ios_base::sync_with_stdio(0);
	lli a,b,c;
	string s;
	cin>>a;
	b = a;
	std::vector<string> v;
	for (int i = 0; i < a; ++i){
		cin>>s;

		v.push_back(s,a);	
	}

	return 0;
}

