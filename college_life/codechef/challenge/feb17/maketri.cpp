#include <iostream>
#include <algorithm>
using namespace std;

typedef long long ll;
typedef long long int lli;
typedef unsigned long long int ulli;
#define max(a,b) return (a>b) ? return a : return b
#define min(a,b) return (a<b) ? return a : return b

int main(int argc, char const *argv[])
{
	ios_base::sync_with_stdio(false);
	ulli a,l,r;
	cin>>a>>l>>r;
	
	return 0;
}
