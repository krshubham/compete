#include<bits/stdc++.h>
using namespace std;

int calculate (string N) {
   
}

int main() {

	ios::sync_with_stdio(0);
	cin.tie(0);
	string N;
	getline(cin, N);

	int out_;
	out_ = calculate(N);
	cout << out_;
}