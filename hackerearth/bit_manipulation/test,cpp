#include<iostream>
using namespace std;
#include<string.h>
class text{
protected:
char str[100];
public:
//Default constructor
text(){}
//Parameterized constructor
text(char*);
void get();
int length();
//function to split a string into many substrings
// delimiter is used to decide the splitting point
// array in which the splitted strings are to be stored
// is also passed as parameter
// the function should return the number of splitted string
// for example if the string is ab$cdf$gh then delimiter is$ and
// splitted strings are ab, cdf, gh and the function must return 3
int split(char delimit,char a[][20]);

};
//Inherit a class dist from text for implementation