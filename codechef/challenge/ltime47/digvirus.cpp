#include <iostream>
#include <string>
using namespace std;

typedef long long ll;
typedef long long int lli;
typedef unsigned long long int ulli;


int main(int argc, char const *argv[]){
	ios_base::sync_with_stdio(0);
	lli t;
	string s;
	cin>>t;
	while(t--){
		cin>>s;
		for (int i = 0; i < s.size(); ++i){
			lli x = static_cast<int>(s[i]) - 48;
			
		}
	}
	return 0;
}