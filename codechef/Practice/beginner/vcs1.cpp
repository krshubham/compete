#include <bits/stdc++.h>
using namespace std;

#define ll long long

int main(int argc, char const *argv[])
{
	std:ios::sync_with_stdio(false);
	ll t;
	//m is ignored
	//k is ignored
	cin>>t;
	while(t--){
		ll n,m,k,ac,bc;
		cin>>n>>m>>k;
		ll ign[m],trk[k];
		for (int i = 0; i < m; ++i){
			cin>>ign[i];
		}
		for (int i = 0; i < k; ++i){
			cin>>trk[i];
		}
		for (int i = 0; i < m; ++i)
		{
			if()
		}
	}
	cout<<a<<endl;
	return 0;
}
